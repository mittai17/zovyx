# Zuvix Anthropic Vertex Provider

Official Zuvix provider plugin for Claude models hosted through Google Vertex AI.

Install from Zuvix:

```bash
zuvix plugin add @zuvix/anthropic-vertex-provider
```

Configure Google Cloud credentials and the target Vertex project/region in Zuvix, then select Claude models with the Anthropic Vertex provider.
