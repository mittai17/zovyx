# Zuvix Matrix

Official Zuvix channel plugin for Matrix rooms and direct messages.

Install from Zuvix:

```bash
zuvix plugin add @zuvix/matrix
```

Configure the Matrix homeserver and bot credentials in Zuvix. The plugin lets agents join configured rooms, receive messages, and reply through Matrix.
