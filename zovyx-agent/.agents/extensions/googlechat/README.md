# Zuvix Google Chat

Official Zuvix channel plugin for Google Chat spaces and direct messages.

Install from Zuvix:

```bash
zuvix plugin add @zuvix/googlechat
```

Configure the Google Chat app credentials and allowed spaces in Zuvix. The plugin lets agents receive Google Chat events and reply through the configured app.
