# Zuvix Discord

Official Zuvix channel plugin for Discord servers, channels, DMs, slash commands, and app events.

Install from Zuvix:

```bash
zuvix plugin add @zuvix/discord
```

Configure a Discord bot token and the channels or servers Zuvix should handle. The plugin lets Zuvix agents receive Discord messages and respond through the configured Discord app.
