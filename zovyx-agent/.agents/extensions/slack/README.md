# Zuvix Slack

Official Zuvix channel plugin for Slack channels, DMs, commands, and app events.

Install from Zuvix:

```bash
zuvix plugin add @zuvix/slack
```

Configure the Slack app credentials and allowed workspaces/channels in Zuvix. The plugin lets agents receive Slack events and reply through the configured Slack app.
