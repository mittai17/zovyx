<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_ZUVIX"
        app:queryPatterns="@array/ask_zuvix_query_patterns">
        <intent
            android:action="ai.zuvix.app.action.ASK_ZUVIX"
            android:targetPackage="ai.zuvix.app"
            android:targetClass="ai.zuvix.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
