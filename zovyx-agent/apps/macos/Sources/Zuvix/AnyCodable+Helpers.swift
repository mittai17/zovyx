import Foundation
import ZuvixKit

// Prefer the ZuvixKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = ZuvixKit.AnyCodable
typealias InstanceIdentity = ZuvixKit.InstanceIdentity
