apiVersion: v1
kind: Service
metadata:
  name: zuvix
  labels:
    app: zuvix
spec:
  type: ClusterIP
  selector:
    app: zuvix
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
