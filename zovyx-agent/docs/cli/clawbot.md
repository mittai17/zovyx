---
summary: "CLI reference for `zuvix clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `zuvix clawbot ...`
  - You need migration guidance to current commands
title: "Clawbot"
---

# `zuvix clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `zuvix clawbot qr` (same behavior as [`zuvix qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `zuvix clawbot qr` -> `zuvix qr`

## Related

- [CLI reference](/cli)
